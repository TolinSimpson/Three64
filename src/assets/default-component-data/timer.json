{
  "type": "Timer",
  "params": {
    "name": "timer",
    "duration": 60,
    "direction": "down",
    "autoStart": false,
    "loop": false,
    "thresholds": [],
    "scope": "object",
    "sync": "none",
    "persist": false
  },
  "paramDescriptions": [
    { "key": "name",       "label": "Name",         "type": "string",  "description": "Unique identifier for this timer." },
    { "key": "duration",   "label": "Duration (s)",  "type": "number",  "min": 0, "step": 1, "description": "Duration in seconds." },
    { "key": "direction",  "label": "Direction",     "type": "string",  "description": "down = countdown, up = countup." },
    { "key": "autoStart",  "label": "Auto Start",    "type": "boolean", "description": "Start ticking on Initialize." },
    { "key": "loop",       "label": "Loop",          "type": "boolean", "description": "Reset and restart on completion." },
    { "key": "thresholds", "label": "Thresholds",    "type": "object",  "description": "Array of { value, event } objects." },
    { "key": "scope",      "label": "Scope",         "type": "string",  "description": "Forwarded to internal Statistic." },
    { "key": "sync",       "label": "Sync",          "type": "string",  "description": "Forwarded to internal Statistic." },
    { "key": "persist",    "label": "Persist",       "type": "boolean", "description": "Forwarded to internal Statistic." }
  ]
}
