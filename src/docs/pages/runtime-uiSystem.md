# runtime/uiSystem.js

## API
- `class UISystem`: Lightweight UI layer (e.g., HUD elements).

## Usage
Use alongside components that render UI (e.g., health bars); wire into your update loop as needed.


