# runtime/loadingScreen.js

## API
- Default export: Loading screen helper used by the engine during heavy loads.

## Usage
Normally managed by the engine. You can display/hide it manually via `app.loading`.


