# runtime/raycaster.js

## API
- `class RaycasterComponent extends Component`: Performs raycasts from an object (e.g., interaction, targeting).

## Usage
Attach to a scene object via GLTF userData or register and add programmatically.


