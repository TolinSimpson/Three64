# runtime/player.js

## API
- `class Player extends Component`: Camera/character wiring for a controllable player.

## Usage
Attach via GLTF userData (see scene conventions) or create programmatically as a component instance.


