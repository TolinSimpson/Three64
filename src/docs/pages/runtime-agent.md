# runtime/agent.js

## API
- `class Agent extends Component`: AI/navigation agent with optional navmesh pathing.

## Usage
Attach to objects via GLTF userData or registry. Enable navmesh in params when needed.


