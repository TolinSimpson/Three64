# runtime/skybox.js

## API
- `createSkybox(camera, options)`: Builds a skybox bound to a camera.

## Usage
```js
import { createSkybox } from './runtime/skybox.js';
const sky = createSkybox(camera, { /* params */ });
```


