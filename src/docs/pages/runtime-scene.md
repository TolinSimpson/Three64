# runtime/scene.js

## API
- `class Scene extends Component`: Scene root; manages component instances and lifecycle.

## Usage
Attach components via GLTF userData or registry, then let the engine load your scene. See `scene-conventions.md`.


